DATABASE_URL=postgresql://user:password@host:5432/postgres
SUPABASE_URL=https://your-project.supabase.co
ANTHROPIC_API_KEY=sk-ant-...
# Comma-separated list of extra allowed CORS origins (production frontend URL(s))
FRONTEND_URL=https://your-app.vercel.app
# Web Push (browser notifications) — generate with scripts/generate_vapid_keys.py
# Both keys are raw, base64url-encoded (no PEM headers, no padding)
VAPID_PRIVATE_KEY=...
VAPID_PUBLIC_KEY=...
VAPID_SUBJECT=mailto:you@example.com
